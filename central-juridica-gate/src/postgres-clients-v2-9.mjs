const clone=v=>structuredClone(v);
export async function ensureClientTable(q){await q.query(`CREATE TABLE IF NOT EXISTS central_juridica_clients(client_id text PRIMARY KEY,name text NOT NULL,type text NOT NULL,status text NOT NULL,payload jsonb NOT NULL,created_at timestamptz NOT NULL,updated_at timestamptz NOT NULL)`);await q.query('CREATE INDEX IF NOT EXISTS central_juridica_clients_status_idx ON central_juridica_clients(status)');await q.query('CREATE INDEX IF NOT EXISTS central_juridica_clients_name_idx ON central_juridica_clients((lower(name)))');}
export async function readClients(q){const r=await q.query('SELECT payload FROM central_juridica_clients ORDER BY created_at DESC, client_id DESC');return(r.rows||[]).map(x=>clone(x.payload));}
export async function findClientById(q,id,{forUpdate=false}={}){const r=await q.query(`SELECT payload FROM central_juridica_clients WHERE client_id=$1${forUpdate?' FOR UPDATE':''}`,[id]);return r.rows?.length?clone(r.rows[0].payload):null;}
const vals=c=>[c.id,String(c.name||''),String(c.type||'Empresa'),String(c.status||'Ativo'),JSON.stringify(c),c.createdAt,c.updatedAt];
export async function insertClient(q,c){await q.query(`INSERT INTO central_juridica_clients(client_id,name,type,status,payload,created_at,updated_at) VALUES($1,$2,$3,$4,$5::jsonb,$6::timestamptz,$7::timestamptz)`,vals(c));return clone(c);}
export async function migrateLegacyClients(pool){const c=await pool.connect();try{await c.query('BEGIN');await ensureClientTable(c);const selected=await c.query('SELECT state FROM central_juridica_state WHERE singleton=TRUE FOR UPDATE');if(!selected.rows?.length)throw new Error('STATE_MISSING');const db=structuredClone(selected.rows[0].state||{}),items=Array.isArray(db.clients)?db.clients:[],dedicated=await readClients(c);if(!dedicated.length)for(const x of items)await insertClient(c,x);else if(items.length){const ids=new Set(dedicated.map(x=>x.id));if(items.some(x=>!ids.has(x.id)))throw new Error('CLIENT_MIGRATION_CONFLICT');}db.clients=[];await c.query('UPDATE central_juridica_state SET state=$1::jsonb,updated_at=now() WHERE singleton=TRUE',[JSON.stringify(db)]);await c.query('COMMIT');return{migrated:items.length};}catch(e){try{await c.query('ROLLBACK');}catch{}throw e;}finally{c.release();}}
