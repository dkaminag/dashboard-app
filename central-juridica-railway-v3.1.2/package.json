{
  "name": "central-juridica-railway-v3-1-2",
  "version": "3.1.2",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node unpack.mjs && npm install --prefix runtime --omit=dev --no-audit --no-fund && node --check start.mjs && node --check runtime/src/server.mjs",
    "start": "node start.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
